# Copy this file to .env and adjust if needed. Never commit .env.

# Where the frontend expects the API to be running. Defaults to
# http://localhost:4000 (see server/README.md) if unset.
VITE_API_URL="http://localhost:4000"
